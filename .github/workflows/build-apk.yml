name: Build VibeMusic3D APK

on:
  push:
    paths:
      - "VibeMusic3D_SongsReady.zip"
      - ".github/workflows/build-apk.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        run: sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.7.1"

      - name: Extract project
        run: |
          rm -rf work
          mkdir work
          unzip -q VibeMusic3D_SongsReady.zip -d work
          test -f work/VibeMusic3D/settings.gradle.kts

      - name: Build debug APK
        working-directory: work/VibeMusic3D
        run: gradle :app:assembleDebug --stacktrace --no-daemon

      - name: Verify APK
        run: |
          test -f work/VibeMusic3D/app/build/outputs/apk/debug/app-debug.apk
          ls -lh work/VibeMusic3D/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: VibeMusic3D-debug-apk
          path: work/VibeMusic3D/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14
